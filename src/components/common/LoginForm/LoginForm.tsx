import React, { Fragment } from "react";
import { Background } from "./LoginStyle";

export const LoginForm = () => {
  return (
    <Fragment>
      <Background>
        <h1>This is Login</h1>
      </Background>
    </Fragment>
  );
};
